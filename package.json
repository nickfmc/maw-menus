{
  "name": "grav-plugin-maw-menus",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Drag-and-drop menu builder for Grav Admin2. Builds admin-next/pages/maw-menus.js and admin-next/fields/menus.js.",
  "scripts": {
    "build": "vite build && vite build -c vite.field.config.js && node scripts/finalize.mjs",
    "watch": "vite build --watch",
    "test": "node --test \"tests/js/*.test.mjs\""
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.28.0",
    "vite": "^6.3.0"
  }
}
